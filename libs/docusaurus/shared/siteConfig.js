/**
 * Single source of truth for site URL configuration.
 * Imported by docusaurus.config.ts, generate-anki-decks.js, and any future consumer.
 * Plain .js to avoid CJS-can't-require-TS problems.
 */
module.exports = {
  url: "https://agentfactory.panaversity.org",
  baseUrl: "/",
};
