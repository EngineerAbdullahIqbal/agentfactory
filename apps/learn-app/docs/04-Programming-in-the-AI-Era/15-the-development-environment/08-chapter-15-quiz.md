---
sidebar_position: 8
title: "Chapter 15: The Development Environment Quiz"
---

# Chapter 15 Quiz

Before taking the quiz, verify that you can do each of these without looking at the lessons:

- [ ] **Create a project**: Run `uv init` and explain what each generated file does
- [ ] **Install dev tools**: Run `uv add --dev` and find the tools in `pyproject.toml`
- [ ] **Read tool output**: Run `uv run ruff check .` or `uv run pyright` and explain what each warning means using the rule code
- [ ] **Run the full pipeline**: Execute `uv run ruff check . && uv run pyright && uv run pytest` and explain why the order matters and what `&&` does
- [ ] **Commit with Git**: Run `git add` and `git commit` and explain why small, frequent commits matter

If any feel uncertain, revisit the specific lesson before continuing.

Test your understanding of the Python discipline stack by solving real-world scenarios. Each question presents a situation you might encounter while working with uv, pyright, ruff, pytest, and Git.

<Quiz
  title="Chapter 15: The Development Environment"
  questions={[
    {
      question: "A developer writes 500 lines of code over two weeks and plans to add tests 'once the code stabilizes.' By week three, a bug report comes in but they cannot remember whether a function was supposed to return a list or a dictionary. Which anti-pattern from the discipline stack caused this problem?",
      options: [
        "The 'test later' anti-pattern, where deferred tests lose the specification of intended behavior",
        "The 'no linter' anti-pattern, where missing lint rules caused the function signature to drift",
        "The 'just pip install' anti-pattern, where global packages caused unexpected return types",
        "The 'no virtual environment' anti-pattern, where system Python changed the function behavior"
      ],
      correctOption: 0,
      explanation: "The 'test later' anti-pattern means specifications fade from memory. If the developer had written 'assert get_notes(\"meeting\") == [{\"title\": \"Meeting\"}]' alongside the code, the test would document exactly what the function returns. Without it, the developer's increasingly uncertain recollection is the only specification. Option B is wrong because linting catches style issues and unused code, not return type expectations. Option C is incorrect because global packages do not change what a function returns. Option D is wrong because virtual environments affect which packages are available, not the behavior of code already written.",
      source: "Lesson 6: Testing With pytest"
    },
    {
      question: "Emma draws five boxes on a whiteboard -- uv, pyright, ruff, pytest, Git -- and says 'removing any one leaves a gap that willpower cannot fill.' James suggests dropping pyright since his code runs fine without types. If the team removes pyright, which specific failure mode opens up?",
      options: [
        "Code reviews will become impossible because reviewers cannot read untyped Python functions at all",
        "The uv.lock file will stop generating because pyright validates each of its dependency entries",
        "Git will refuse to commit any Python files that lack type annotations in their function signatures",
        "Type errors will hide until runtime, surfacing as crashes in production instead of the editor"
      ],
      correctOption: 3,
      explanation: "Without pyright, a function expecting a string can silently receive an integer, producing wrong output or crashing at runtime. Pyright catches these mismatches before the code runs, in the editor, in seconds. Removing it means type errors hide until they cause real damage. Option A is wrong because code reviews work on any code -- types make reviews easier but are not required for them. Option B is incorrect because uv.lock is generated by uv based on dependency resolution, not by pyright. Option C is wrong because Git tracks all files regardless of type annotations. This is Axiom V (Types Are Guardrails) losing its enforcement tool.",
      source: "Lesson 1: Why the Toolchain Comes First"
    },
    {
      question: "James writes a Python script that imports requests, prints a greeting, and pushes it to the team repo. The next morning, Emma pulls the code and sees 'ModuleNotFoundError: No module named requests'. James says 'But it runs on my machine.' What is the root cause?",
      options: [
        "Emma has a firewall blocking pip from downloading the requests package automatically",
        "The requests library was deprecated overnight and removed from the PyPI package index",
        "Emma needs to run the script with administrator privileges to access installed packages",
        "James installed requests globally instead of inside a project-managed virtual environment"
      ],
      correctOption: 3,
      explanation: "James installed requests into his global Python environment, which means it exists only on his machine. Without a pyproject.toml listing requests as a dependency and a virtual environment isolating the project, there is no way for Emma's machine to know the package is needed. Option A is wrong because the error is ModuleNotFoundError, not a network error -- the package was never installed, not blocked from downloading. Option B is incorrect because requests is a stable, widely-used library that was not removed. Option C is wrong because Python package imports do not require administrator privileges. In professional development, uv add requests would record the dependency.",
      source: "Lesson 2: Installing uv and Creating SmartNotes"
    },
    {
      question: "An AI assistant generates 50 lines of Python in seconds. The developer reads through the code for ten minutes looking for problems but misses an unused import on line 3 and a wrong variable name on line 41. Which discipline stack principle explains why reading alone was insufficient?",
      options: [
        "Tools replace willpower with infrastructure -- automated checks catch what human review misses at speed",
        "AI-generated code cannot be read by humans because it uses internal optimization patterns",
        "The developer should have asked a different AI to review the first AI's generated output",
        "AI-generated code requires a special compiler flag that enables stricter syntax validation rules"
      ],
      correctOption: 0,
      explanation: "The discipline stack principle is that tools replace willpower with infrastructure. AI generates code faster than humans can review it, so automated verification (ruff for the unused import, pyright for type mismatches, pytest for behavior) provides the safety net at matching speed. Option B is wrong because AI-generated code is standard Python that humans can read. Option C replaces one manual review with another, still relying on attention rather than automation. Option D is fabricated -- Python has no special compiler flags for AI-generated code. The lesson explicitly states that the discipline stack matters more in the AI era because verification must match generation speed.",
      source: "Lesson 4: Ruff -- Your Code Quality Guardian"
    },
    {
      question: "A team of five developers works without a linter. During code review, the reviewer spends 25 minutes pointing out inconsistent spacing, unused imports, and quote style differences. None of these comments are about logic or design. Which discipline stack tool would have prevented this wasted review time?",
      options: [
        "pytest, because it verifies code formatting as part of its test assertion checks",
        "Git, because it rejects commits that contain formatting inconsistencies automatically",
        "pyright, because it enforces consistent code style through type annotation analysis",
        "ruff, because it catches style violations and unused imports before code review starts"
      ],
      correctOption: 3,
      explanation: "ruff catches unused imports, inconsistent spacing, and style violations automatically before code reaches review. Running ruff check and ruff format ensures mechanical quality issues are resolved instantly, freeing reviewers to focus on design and logic. Option A is wrong because pytest checks behavioral correctness, not formatting or imports. Option B is incorrect because Git records changes but does not analyze code quality or reject commits based on style. Option C is wrong because pyright checks type annotations, not spacing or import hygiene. The lesson emphasizes that ruff is the first stage of the verification pipeline.",
      source: "Lesson 4: Ruff -- Your Code Quality Guardian"
    },
    {
      question: "James installs packages globally with pip and skips creating a virtual environment. After three months, two of his projects need conflicting versions of the same library. Which anti-pattern combination caused this version conflict?",
      options: [
        "Installing packages with pip directly combined with running code without any virtual environment isolation",
        "Configuring too many linting rules in the ruff settings for each individual project separately",
        "Using strict mode in pyright instead of the standard type checking mode for analysis tasks",
        "Committing the virtual environment directory into the Git repository alongside the source code"
      ],
      correctOption: 0,
      explanation: "Two anti-patterns combined: 'Just pip install' (global package installation) and 'No virtual environment' (no project isolation). When packages install globally, all projects share one environment, making version conflicts inevitable. Option B is unrelated -- ruff rules do not affect dependency management. Option C concerns type checking strictness, not package isolation. Option D is a different mistake (committing generated files), but it would not cause version conflicts between projects. The fix is to use uv add inside each project so dependencies install into isolated environments.",
      source: "Lesson 1: Why the Toolchain Comes First"
    },
    {
      question: "A developer maps each discipline stack tool to its axiom. They write: uv = Axiom V (Types Are Guardrails), pyright = Axiom I (Shell as Orchestrator). Emma says both mappings are wrong. What are the correct mappings?",
      options: [
        "uv = Axiom VII (Tests Are the Specification), pyright = Axiom IX (Verification is Pipeline)",
        "uv = Axiom I (Shell as Orchestrator), pyright = Axiom V (Types Are Guardrails)",
        "uv = Axiom VIII (Version Control is Memory), pyright = Axiom VII (Tests Are the Specification)",
        "uv = Axiom IX (Verification is Pipeline), pyright = Axiom VIII (Version Control is Memory)"
      ],
      correctOption: 1,
      explanation: "uv implements Axiom I (Shell as Orchestrator) because one shell command orchestrates Python versions, virtual environments, dependencies, and script execution. pyright implements Axiom V (Types Are Guardrails) because it walks along every type annotation verifying that data types match. The developer had the tools swapped. Option A is wrong because tests map to pytest, not uv, and pipeline maps to ruff, not pyright. Option C is wrong because version control maps to Git, not uv. Option D is wrong because verification pipeline maps to ruff, not uv. The axiom-to-tool mapping is: uv=I, pyright=V, pytest=VII, Git=VIII, ruff=IX.",
      source: "Lesson 2: Installing uv and Creating SmartNotes"
    },
    {
      question: "James argues that ruff (Axiom IX) and pytest (Axiom VII) cover the same ground because both find problems in code. Emma disagrees. Who is correct, and what is the key distinction?",
      options: [
        "James is correct because ruff's F841 rule detects the same behavioral bugs that pytest catches",
        "Neither is correct because Git is the only tool that truly verifies code quality through history",
        "James is correct because running ruff check with the fix flag auto-generates test assertions",
        "Emma is correct because ruff finds structural problems while pytest verifies correct behavior"
      ],
      correctOption: 3,
      explanation: "Emma is correct. ruff checks code structure and style -- unused imports, undefined names, formatting consistency -- while pytest checks behavior -- does the function return the right value for the right input. A function can pass all ruff checks but return wrong results, and a function can pass all tests but have unused imports. They cover non-overlapping categories. Option A is wrong because F841 detects unused variables, not behavioral bugs. Option B is wrong because Git preserves history but does not verify correctness. Option C is wrong because ruff does not generate test assertions. The pipeline works because each tool covers a different gap.",
      source: "Lesson 6: Testing With pytest"
    },
    {
      question: "A developer considers installing Python the traditional way, saying 'I will just download it from python.org.' Emma says uv replaces that entire workflow. The developer has python.org open in their browser. What should they do instead?",
      options: [
        "Download the installer but skip adding Python to PATH to avoid conflicts with other tools",
        "Use pip to install uv, then let uv manage all future Python version installations needed",
        "Run the uv installation command for their platform, then use uv init to start the project",
        "Install Python from python.org first, then install uv as an add-on package after setup"
      ],
      correctOption: 2,
      explanation: "The discipline stack approach is to install uv first, which then handles Python version management per-project through .python-version. Running the platform-specific uv install command and then uv init creates a project with the correct Python version pinned automatically. Option A still uses the manual python.org workflow that uv replaces. Option B is a valid bootstrap method but understates the immediate next step. Option D installs Python globally first, which is the anti-pattern uv eliminates. The lesson shows Emma stopping James before he downloads from python.org, directing him to install uv instead.",
      source: "Lesson 2: Installing uv and Creating SmartNotes"
    },
    {
      question: "A teammate runs 'source .venv/bin/activate' before every command in the SmartNotes project. Another teammate on Windows tries the same command and gets an error. According to the discipline stack, what should both teammates do instead?",
      options: [
        "Both should use 'uv run' before their commands, which handles the environment automatically on all platforms",
        "The Windows teammate should use '.venv\\Scripts\\activate' while the Mac teammate keeps using source",
        "Both should install virtualenv separately since the built-in venv module has platform compatibility bugs",
        "Both should add .venv/bin to their system PATH permanently so activation becomes unnecessary"
      ],
      correctOption: 0,
      explanation: "uv run handles virtual environment management automatically and identically on all platforms. No activation command is needed -- uv run verifies the Python version, syncs the environment, and runs the command inside it. Option B solves the immediate error but keeps the anti-pattern of manual activation, which is platform-specific and easy to forget. Option C adds another tool instead of using the one already in the discipline stack. Option D modifies the system PATH permanently, which is a different anti-pattern that breaks project isolation. The lesson explicitly identifies manual venv activation as an anti-pattern.",
      source: "Lesson 2: Installing uv and Creating SmartNotes"
    },
    {
      question: "A developer runs 'uv run main.py' for the first time on a freshly created SmartNotes project. Before this command, the project had five files. After the command finishes, two new items appear. The developer wants to push to Git. Which item should they commit and which should they exclude?",
      options: [
        "Commit both .venv/ and uv.lock because both are needed to reproduce the environment exactly",
        "Exclude both .venv/ and uv.lock because both are regenerated automatically by uv on demand",
        "Commit .venv/ for environment consistency, exclude uv.lock because it changes too frequently",
        "Commit uv.lock for version reproducibility, exclude .venv/ because it is a generated artifact"
      ],
      correctOption: 3,
      explanation: "uv.lock records exact dependency versions and must be committed so every developer gets identical packages via uv sync. The .venv/ directory is a machine-specific generated artifact that uv recreates from pyproject.toml and uv.lock, so it should be excluded (the .gitignore already handles this). Option A is wrong because .venv/ is large, machine-specific, and regenerable. Option B is wrong because excluding uv.lock means different machines could resolve different package versions. Option C reverses the correct policy -- .venv/ should never be committed while uv.lock always should.",
      source: "Lesson 2: Installing uv and Creating SmartNotes"
    },
    {
      question: "After running 'uv init smartnotes', a developer lists the files and sees .gitignore, .python-version, README.md, main.py, and pyproject.toml. They open .python-version and see a single line: '3.12'. Their teammate has Python 3.10 and 3.12 both installed. What does this file guarantee when the teammate clones the project?",
      options: [
        "uv will automatically use Python 3.12 for this project regardless of other installed versions",
        "It forces the teammate to uninstall Python 3.10 before they can work on the project code",
        "It displays a warning message but allows any Python version to run the project code",
        "It adds Python 3.12 to the system PATH so all terminal commands default to that version"
      ],
      correctOption: 0,
      explanation: "The .python-version file contains one line (3.12) that uv reads on every command. When the teammate runs uv run main.py, uv uses Python 3.12 specifically for this project, even though 3.10 is also installed. Per-project version pinning means different projects can use different Python versions without conflict. Option B is wrong because uv does not require uninstalling other versions. Option C is wrong because uv enforces the version, not just warns about it. Option D is wrong because .python-version is project-scoped, not system-wide -- it does not modify PATH.",
      source: "Lesson 2: Installing uv and Creating SmartNotes"
    },
    {
      question: "A developer's .python-version says 3.12, but their pyproject.toml says requires-python = '>=3.10'. Emma says these are not contradictory. James says they should match exactly. Who is correct and why?",
      options: [
        "James is correct because mismatched versions cause uv to crash when resolving package dependencies",
        "Emma is correct because .python-version pins the local version while requires-python sets the minimum for all users",
        "James is correct because PEP 621 requires both files to specify the identical Python version number",
        "Emma is correct because uv ignores .python-version and only reads requires-python for version selection"
      ],
      correctOption: 1,
      explanation: "Emma is correct. .python-version pins the exact Python version uv uses locally (3.12), while requires-python in pyproject.toml declares the minimum version the project supports (any version 3.10 or higher). A user with Python 3.10 can run the project, but the development team uses 3.12. These serve different purposes and are not contradictory. Option A is wrong because uv handles both files without crashing. Option C is wrong because PEP 621 defines project metadata format, not version matching rules. Option D is wrong because uv reads both files.",
      source: "Lesson 2: Installing uv and Creating SmartNotes"
    },
    {
      question: "A developer runs 'uv run main.py' in the SmartNotes project. Behind the scenes, uv performs several steps before executing the file. Which sequence accurately describes what uv did?",
      options: [
        "Downloaded pip, created requirements.txt, activated the venv, then ran the file using Python",
        "Checked .python-version for the version, synced the environment, installed dependencies, then ran the file",
        "Compiled main.py to bytecode, linked it against system libraries, then executed the compiled output",
        "Pinged the PyPI server to check for package updates, rebuilt the lockfile, then ran the file"
      ],
      correctOption: 1,
      explanation: "uv run performs a specific sequence: verify the Python version from .python-version, create or sync the .venv directory, install any missing dependencies from pyproject.toml, then execute the file. This is Axiom I (Shell as Orchestrator) in practice -- one command handles everything. Option A describes the old pip workflow that uv replaces. Option C describes a compiled language workflow, not Python. Option D fabricates a server-ping step that uv does not perform on every run. The lesson emphasizes that uv run replaces five separate manual steps.",
      source: "Lesson 2: Installing uv and Creating SmartNotes"
    },
    {
      question: "A developer types 'pip install pytest' in the SmartNotes project instead of 'uv add --dev pytest'. The package installs successfully. What three problems has this created compared to using uv add?",
      options: [
        "pyproject.toml was not updated, uv.lock was not regenerated, and the .venv may be out of sync",
        "pip installed a different version, broke the lockfile, and corrupted the virtual environment entirely",
        "pip added pytest to production dependencies, skipped type checking, and disabled ruff configuration",
        "pip installed pytest globally, modified the system PATH, and removed the .python-version file"
      ],
      correctOption: 0,
      explanation: "uv add --dev performs three synchronized operations: updating pyproject.toml, regenerating uv.lock, and syncing .venv. Using pip directly bypasses all three -- pyproject.toml does not know about pytest, uv.lock does not record the exact version, and the environment may drift from the declared configuration. Option B overstates the damage -- pip does not corrupt the venv, but it does bypass the config sync. Option C is wrong because pip install does not interact with pyright or ruff at all. Option D is wrong because pip install inside a venv does not install globally or modify .python-version.",
      source: "Lesson 2: Installing uv and Creating SmartNotes"
    },
    {
      question: "A developer runs 'uv add --dev pytest pyright ruff' in SmartNotes. They open pyproject.toml and see a new [dependency-groups] section. They also notice uv.lock was updated. What would happen if they deleted uv.lock and ran 'uv sync'?",
      options: [
        "uv would refuse to run because the lockfile is a required file that cannot be regenerated",
        "The project would permanently lose track of installed versions and break beyond possible repair",
        "uv would regenerate uv.lock by resolving versions from pyproject.toml, possibly picking newer ones",
        "uv would install the tools without a lockfile, making the environment identical to pip install"
      ],
      correctOption: 2,
      explanation: "uv regenerates the lockfile by reading pyproject.toml and resolving compatible versions. Since pyproject.toml says pytest>=9.0.2, uv might resolve to 9.0.3 or 9.1.0 if a newer version exists. This is why uv.lock should be committed to Git -- it preserves the exact resolution. Option A is wrong because uv.lock is regenerable, not irreplaceable. Option B overstates the damage -- the project recovers, but exact versions may differ. Option D is wrong because uv sync still uses pyproject.toml for configuration and creates a proper lockfile. The lesson explains that pyproject.toml is the specification and uv.lock is the resolution.",
      source: "Lesson 3: pyproject.toml and Discipline Stack"
    },
    {
      question: "A developer's pyproject.toml has [tool.pyright] with typeCheckingMode = 'strict'. Their teammate creates a separate pyrightconfig.json file with typeCheckingMode = 'standard'. Which configuration will pyright actually use?",
      options: [
        "pyright will merge both configurations and use the stricter setting from either file automatically",
        "pyright will use pyrightconfig.json because it takes priority over pyproject.toml for type checking",
        "pyright will fail with an error because two conflicting configuration files cannot coexist in one project",
        "pyright will use pyproject.toml because the [tool.pyright] section is the standard project configuration"
      ],
      correctOption: 1,
      explanation: "When pyrightconfig.json exists, pyright uses it instead of the [tool.pyright] section in pyproject.toml. The teammate's separate file would override the project's strict mode with standard mode, silently reducing type checking coverage. This is exactly the scattered-configuration problem that pyproject.toml solves. Option A is wrong because pyright does not merge configurations -- one file takes priority. Option C is wrong because pyright does not error on dual configs, it just picks one. Option D is wrong because pyrightconfig.json actually takes precedence. The lesson warns against creating separate config files per tool.",
      source: "Lesson 3: pyproject.toml and Discipline Stack"
    },
    {
      question: "Before pyproject.toml, a Python project had requirements.txt, .flake8, pyrightconfig.json, pytest.ini, setup.cfg, and setup.py. A new developer joins the team and asks 'Where is the project configured?' What is the honest answer for each approach?",
      options: [
        "Old: configuration is in setup.py only. New: configuration is in pyproject.toml only and nothing else",
        "Old: there is no configuration because Python projects do not need configuration files to operate",
        "Old: six files in three formats scattered across the root. New: one file in TOML format for everything",
        "Old: configuration is auto-detected by each tool. New: developers must write all of it by hand"
      ],
      correctOption: 2,
      explanation: "The old approach scattered configuration across six files in INI, TOML, JSON, and Python formats. A new developer had to find and parse all of them to understand the project. With pyproject.toml, every dependency, tool setting, and project detail lives in one TOML file. Option A is wrong because setup.py was just one of six files, not the sole configuration source. Option B is wrong because Python projects need configuration. Option D reverses reality -- pyproject.toml is simpler to maintain. The lesson shows that the old approach required reading six files in three different formats.",
      source: "Lesson 3: pyproject.toml and Discipline Stack"
    },
    {
      question: "A developer sees that pyproject.toml has both [project] dependencies = [] and [dependency-groups] dev = ['pytest>=9.0.2']. They wonder why pytest is not in the dependencies list. What would go wrong if they moved pytest into the [project] dependencies instead?",
      options: [
        "pytest would stop working entirely because it can only be installed through dependency-groups",
        "Users installing SmartNotes in production would also unnecessarily download testing and linting tools",
        "The uv.lock file would reject pytest as a production dependency and refuse to resolve versions",
        "pyright would flag the dependency as a type error because pytest is not a runtime library"
      ],
      correctOption: 1,
      explanation: "The [project] dependencies list is for packages that end users need to run the application. If pytest, pyright, and ruff were listed there, anyone installing SmartNotes would also download testing and linting tools they do not need. The [dependency-groups] dev section keeps development tools separate from production requirements. Option A is wrong because pytest works from either section -- the issue is distribution, not functionality. Option C is wrong because uv.lock resolves dependencies from any section. Option D is wrong because pyright does not validate dependency placement.",
      source: "Lesson 3: pyproject.toml and Discipline Stack"
    },
    {
      question: "The SmartNotes pyproject.toml has [tool.ruff.lint] select = ['E', 'F', 'I', 'UP', 'B', 'SIM']. A developer removes this line entirely and runs ruff check. Ruff still works but catches fewer problems. What happened?",
      options: [
        "Ruff fell back to its default rules (E4, E7, E9, F), missing import sorting and bugbear checks",
        "Ruff disabled all checking because the select line is required for the tool to function at all",
        "Ruff switched to formatting-only mode because linting requires an explicit rule selection to run",
        "Ruff checked all available rules instead, which would catch more problems than the selected set"
      ],
      correctOption: 0,
      explanation: "Without an explicit select configuration, ruff falls back to its default rule set (E4, E7, E9, and F rules only). The SmartNotes config adds I (import sorting), UP (Python modernization), B (bugbear common mistakes), and SIM (simplification) -- all of which are lost when the line is removed. Option B is wrong because ruff works without a select line, just with fewer rules. Option C is wrong because ruff check always performs linting regardless. Option D is wrong because the default is a smaller subset, not all rules.",
      source: "Lesson 3: pyproject.toml and Discipline Stack"
    },
    {
      question: "A developer's pyproject.toml says requires-python = '>=3.12'. A teammate tries to run the project with Python 3.11 using uv run main.py. What happens?",
      options: [
        "The code runs normally because requires-python is only a suggestion that uv does not enforce at all",
        "uv automatically downloads and installs Python 3.12 without informing the teammate about the change",
        "uv refuses to run because the project requires Python 3.12 or higher and 3.11 does not qualify",
        "Python 3.11 runs the code but silently disables all type checking and linting on that machine"
      ],
      correctOption: 2,
      explanation: "uv enforces the requires-python constraint. When the project specifies >=3.12 and the teammate only has 3.11 available, uv will not proceed with an incompatible version. The teammate needs Python 3.12 or higher installed. Option A is wrong because uv does enforce this constraint. Option B is partially possible depending on uv configuration, but the default behavior is to error when the constraint is not met. Option D is wrong because Python version differences affect syntax and features, and uv prevents the mismatch from occurring.",
      source: "Lesson 3: pyproject.toml and Discipline Stack"
    },
    {
      question: "Each tool in pyproject.toml gets its own namespace under [tool]. For example, [tool.ruff] and [tool.pyright] are separate sections. What problem does this namespace convention prevent?",
      options: [
        "It prevents uv from installing tools that have conflicting version requirements with each other",
        "It prevents Git from tracking configuration changes that affect multiple different tools simultaneously",
        "It prevents the Python interpreter from loading tool configurations during normal code execution time",
        "It prevents different tools from accidentally overwriting each other's configuration settings in the file"
      ],
      correctOption: 3,
      explanation: "The [tool.*] namespace convention gives each tool its own section -- [tool.pyright], [tool.ruff], [tool.pytest.ini_options] -- so a ruff setting like 'line-length' never collides with a hypothetical pyright setting of the same name. Option A is wrong because namespaces address configuration collision, not version resolution. Option B is incorrect because Git tracks all changes regardless of namespacing. Option C is wrong because the Python interpreter does not read [tool.*] sections at runtime. The lesson explains that each tool gets its own namespace so configurations never collide.",
      source: "Lesson 3: pyproject.toml and Discipline Stack"
    },
    {
      question: "A developer runs 'uv run ruff check .' and sees: 'main.py:1:8: F401 [*] `os` imported but unused'. They also see 'main.py:6:5: F841 Local variable `temp_result` is assigned to but never used'. They run 'uv run ruff check --fix .' to fix both. What is the result?",
      options: [
        "Both issues are fixed automatically because the --fix flag resolves all errors that ruff detects in code",
        "Neither issue is fixed because --fix only works on formatting issues found by the ruff format command",
        "The F401 unused import is removed but the F841 unused variable remains because it requires human judgment",
        "The F841 unused variable is removed but the F401 unused import remains because imports need manual review"
      ],
      correctOption: 2,
      explanation: "ruff --fix removes the unused import (F401) because removing an import is always safe -- imports have no side effects on program logic. The unused variable (F841) remains because removing the assignment could skip a function call with side effects in other contexts. Ruff is conservative: auto-fix when safe, flag when human judgment is needed. Option A is wrong because not all errors are auto-fixable. Option B is wrong because --fix works on linting issues, not just formatting. Option D reverses which issue is fixable -- the [*] marker on F401 indicates it is auto-fixable.",
      source: "Lesson 4: Ruff -- Your Code Quality Guardian"
    },
    {
      question: "A developer sees ruff output showing rules F401, E501, I001, and B006. They know F = Pyflakes and E = pycodestyle. What do I and B represent?",
      options: [
        "I = isort (import sorting order), B = flake8-bugbear (common programming mistakes)",
        "I = inspect (runtime debugging tools), B = built-in (Python standard library rules)",
        "I = isort (import sorting order), B = flake8-bandit (security vulnerability detection)",
        "I = indent (indentation style checking), B = flake8-bugbear (common programming mistakes)"
      ],
      correctOption: 0,
      explanation: "I stands for isort, which enforces import sorting order -- keeping import lines organized alphabetically and by group. B stands for flake8-bugbear, which catches common programming mistakes and design problems that are technically valid Python but almost always indicate bugs. Option B fabricates both definitions. Option C is wrong because B stands for bugbear, not bandit (bandit uses the S prefix). Option D is wrong because I stands for isort, not indent -- indentation checking falls under E (pycodestyle) rules. The lesson provides a complete prefix reference table.",
      source: "Lesson 4: Ruff -- Your Code Quality Guardian"
    },
    {
      question: "A CI pipeline runs 'uv run ruff format --check .' and the command exits with a non-zero code. No files were modified. The build fails. A junior developer asks why ruff did not just fix the formatting automatically. What is the explanation?",
      options: [
        "The --check flag is broken in CI environments and needs to be replaced with the --fix flag instead",
        "The --check flag verifies formatting without modifying files, so the developer must run ruff format locally before pushing",
        "The CI server lacks write permissions to modify files, which is why the formatting was not applied automatically",
        "The non-zero exit code indicates a ruff configuration error, not an actual formatting discrepancy in files"
      ],
      correctOption: 1,
      explanation: "The --check flag is designed for CI: it checks whether files need reformatting without actually modifying them. A non-zero exit code means at least one file does not match the configured style. The developer must run ruff format locally, commit the changes, and push again. Option A is wrong because --check is working as designed. Option C is wrong because the issue is --check's design, not permissions. Option D is wrong because a non-zero exit from --check specifically means files need reformatting. The lesson explains this CI workflow explicitly.",
      source: "Lesson 4: Ruff -- Your Code Quality Guardian"
    },
    {
      question: "A developer runs 'uv run ruff format .' on their SmartNotes code. Before formatting, the code had 'def search_notes(notes,query):' and 'results=[]'. After formatting, these became 'def search_notes(notes, query):' and 'results = []'. The developer worries the changes might break their code. Should they be concerned?",
      options: [
        "No, formatting changes are purely cosmetic and never alter how Python executes the program at all",
        "Yes, adding spaces can change how Python interprets operator precedence in complex expressions",
        "Yes, ruff format sometimes changes variable names to match PEP 8 naming convention standards",
        "No, but only because the pyproject.toml has the correct ruff configuration for safe formatting"
      ],
      correctOption: 0,
      explanation: "Formatting changes are purely cosmetic. Adding a space after a comma or around an equals sign has zero effect on how Python executes the code. Ruff format adjusts whitespace, quote styles, and line breaks for consistency and readability, but never changes program behavior. Option B is wrong because spaces in these contexts do not affect Python's interpretation. Option C is wrong because ruff format does not rename variables. Option D is wrong because ruff format is always behavior-preserving regardless of configuration. The lesson explicitly states that formatting changes affect readability, not execution.",
      source: "Lesson 4: Ruff -- Your Code Quality Guardian"
    },
    {
      question: "Ruff replaces Flake8, Black, isort, pyupgrade, autoflake, and flake8-bugbear. A developer notices ruff runs in milliseconds while Flake8 alone took several seconds on the same codebase. What is the primary reason for this speed difference?",
      options: [
        "Ruff only checks the first 100 lines of each file while Flake8 checks the entire file contents",
        "Ruff delegates analysis to a cloud service that processes files using distributed computing clusters",
        "Ruff uses an aggressive caching strategy that skips files modified within the last five minutes",
        "Ruff is written in Rust, a compiled systems language, while Flake8 is written in interpreted Python"
      ],
      correctOption: 3,
      explanation: "Ruff achieves 10-100x speed improvement because it is written in Rust, a compiled systems language. Compiled Rust code executes much faster than interpreted Python for text analysis tasks. Both ruff and Flake8 check entire files. Option A is wrong because ruff checks entire files. Option B is wrong because ruff runs entirely locally with no cloud dependency. Option C fabricates a caching strategy that does not explain the fundamental speed difference. The lesson attributes ruff's speed to being written in Rust by Astral.",
      source: "Lesson 4: Ruff -- Your Code Quality Guardian"
    },
    {
      question: "A developer runs ruff check and sees two issues: 'main.py:1:8: F401 [*] os imported but unused' and 'main.py:10:5: E501 Line too long (112 > 88 characters)'. They want to prioritize. Which issue is more likely to indicate an actual bug?",
      options: [
        "E501 is more serious because long lines crash Python's parser when they exceed 88 total characters",
        "Both are equally serious because all ruff rules indicate potential bugs in the actual code logic",
        "F401 is more likely a bug indicator because dead code may signal a missing usage or wrong import",
        "Neither indicates a bug because ruff only catches formatting issues with no impact on behavior"
      ],
      correctOption: 2,
      explanation: "F401 (unused import) is more likely to indicate a real problem -- the developer may have imported the wrong module, forgotten to use it, or left a stale import after refactoring. E501 (line too long) is purely a readability concern and never indicates a bug. Option A is wrong because Python does not crash on long lines -- 88 is a convention, not a language limit. Option B is wrong because lint and formatting issues have different severity. Option D is wrong because ruff check catches actual bugs (F rules) in addition to style issues (E rules).",
      source: "Lesson 4: Ruff -- Your Code Quality Guardian"
    },
    {
      question: "A developer adds '# noqa: F401' to every unused import in their codebase instead of removing them. Their codebase has 47 noqa comments suppressing various warnings. What is the consequence of this practice?",
      options: [
        "The noqa comments cause ruff to run significantly slower because each comment requires extra processing",
        "Python's interpreter will raise syntax errors when it encounters noqa comments in production code",
        "The noqa comments are automatically removed by ruff format on the next formatting pass through",
        "Real problems like undefined names and actual bugs are hidden behind blanket suppression noise"
      ],
      correctOption: 3,
      explanation: "Blanket noqa suppression defeats the purpose of having a linter. Warnings like F401 (unused imports), F841 (unused variables), and B006 (mutable defaults) exist because they often indicate real problems. Suppressing them all hides genuine bugs behind comment noise. Option A is wrong because noqa comments have negligible performance impact. Option B is wrong because Python ignores comments entirely at runtime. Option C is wrong because ruff format does not remove noqa comments. The lesson explicitly lists noqa everywhere as an anti-pattern.",
      source: "Lesson 4: Ruff -- Your Code Quality Guardian"
    },
    {
      question: "A developer runs ruff check and sees 'main.py:3:25: F401 [*] `collections.OrderedDict` imported but unused'. They look at the output format 'file:line:column: RULE_CODE message'. Which part of this output lets them navigate directly to the problem in their editor?",
      options: [
        "The file:line:column reference like main.py:3:25, which gives the exact location in the source code file",
        "The message text, which describes the problem well enough to find it by reading the file",
        "The collections.OrderedDict name, which can be searched using the find-replace tool in editors",
        "The F401 rule code, which links to the official ruff documentation page for reference lookup"
      ],
      correctOption: 0,
      explanation: "The file:line:column reference (main.py:3:25) enables direct navigation to the exact position in any editor using go-to-line shortcuts. This is the most actionable part of ruff output for locating problems instantly. Option B helps understanding but not precise navigation -- reading through a file is slower than jumping to coordinates. Option C could work but is less direct than using exact coordinates. Option D links to documentation for understanding the rule, not for locating the issue in your editor. The lesson demonstrates this output format and explains each component.",
      source: "Lesson 4: Ruff -- Your Code Quality Guardian"
    },
    {
      question: "James writes 'def greet(name):' without type annotations. He calls greet(42) and Python prints 'Hello, 42' without any error. Emma writes the same function as 'def greet(name: str) -> str:' and runs pyright. Pyright catches the bug. What did the type annotations enable that plain Python could not?",
      options: [
        "The annotations made Python run the function faster by pre-allocating memory for the string type",
        "The annotations allowed pyright to detect the int-to-str mismatch before the code ever executed",
        "The annotations forced Python to convert the integer 42 to the string '42' automatically at runtime",
        "The annotations caused Python's interpreter to raise a TypeError when greet(42) was called directly"
      ],
      correctOption: 1,
      explanation: "Type annotations like ': str' and '-> str' give pyright enough information to detect that 42 (an int) does not match the expected str parameter. This detection happens statically -- without running the code. Without annotations, pyright has nothing to check, and Python happily produces wrong output. Option A is wrong because Python ignores annotations at runtime. Option C is wrong because Python does not auto-convert types based on annotations. Option D is wrong because Python's interpreter ignores type annotations completely. The lesson shows this exact before/after comparison.",
      source: "Lesson 5: Pyright -- Your Type Safety Net"
    },
    {
      question: "A developer runs 'uv run pyright' and sees: 'main.py:19:26 - error: Argument of type \"str\" is not assignable to parameter \"a\" of type \"int\" (reportArgumentType)'. The function signature is 'def add_numbers(a: int, b: int) -> int'. What call caused this error?",
      options: [
        "add_numbers(10, 20) because both arguments should be strings for proper concatenation in this context",
        "add_numbers('five', 10) because the string 'five' is not assignable to the int parameter 'a' here",
        "add_numbers(5, 10) because the sum of two integers cannot be assigned to an int return type",
        "add_numbers(5.0, 10.0) because float values are not assignable to int parameter declarations here"
      ],
      correctOption: 1,
      explanation: "The error message says 'Argument of type str is not assignable to parameter a of type int' at column 26, which points to the first argument. This means a string value was passed where parameter 'a' expects an int. The call add_numbers('five', 10) passes the string 'five' as the first argument. Option A is wrong because (10, 20) are both ints matching the signature. Option C is wrong because (5, 10) matches the signature perfectly. Option D describes a different mismatch that would produce a different error. The lesson walks through this exact error.",
      source: "Lesson 5: Pyright -- Your Type Safety Net"
    },
    {
      question: "A developer writes 'length: int = format_title(\"My Note\")' where format_title returns str. Pyright reports: 'Type \"str\" is not assignable to declared type \"int\" (reportAssignmentType)'. Another developer says this is the same rule as reportArgumentType. Are they correct?",
      options: [
        "Yes, both rules detect the same category of type mismatch between str and int values always",
        "Yes, pyright uses both names interchangeably depending on which line the error appears on",
        "No, reportArgumentType is only for strict mode while reportAssignmentType works in all modes",
        "No, reportArgumentType catches wrong data passed to functions while reportAssignmentType catches wrong data in variables"
      ],
      correctOption: 3,
      explanation: "These are distinct rules catching different categories of mismatch. reportArgumentType fires when a value passed as an argument does not match the parameter's declared type. reportAssignmentType fires when a value being stored does not match the variable's declared type. Option A is wrong because they cover different mismatch locations. Option B is wrong because pyright uses consistent, specific rule names. Option C is wrong because both rules function in strict and standard modes. The lesson demonstrates both rules with separate examples.",
      source: "Lesson 5: Pyright -- Your Type Safety Net"
    },
    {
      question: "A function signature reads 'def format_title(title: str, prefix: str | None = None) -> str'. A developer calls format_title(\"My Note\", None). Another developer says passing None should cause a type error. Who is correct?",
      options: [
        "The second developer is correct because None is not a valid string value for any parameter type",
        "Neither is correct because pyright does not check default parameter values at all in any mode",
        "The first developer is correct because str | None explicitly declares that None is an accepted value for prefix",
        "The second developer is correct because the default value None means the parameter must be omitted entirely"
      ],
      correctOption: 2,
      explanation: "The union type str | None means the prefix parameter explicitly accepts either a string or None. Passing None is perfectly valid -- it matches the None part of the union type. Option A is wrong because str | None specifically includes None as an allowed type. Option B is wrong because pyright checks argument types including None. Option D is wrong because the default value means the parameter is optional, but passing None explicitly is also valid. The lesson explains that str | None makes optional values explicit in a type-safe way.",
      source: "Lesson 5: Pyright -- Your Type Safety Net"
    },
    {
      question: "A developer switches pyright from strict mode to standard mode. In strict mode they had 8 errors. In standard mode they have 2 errors. Their teammate argues standard mode is better because it shows fewer errors. Why is this reasoning flawed?",
      options: [
        "Standard mode is actually stricter than strict mode and catches more subtle type errors overall",
        "The 6 fewer errors represent real coverage gaps where functions lack type annotations and go unchecked",
        "Standard mode runs slower than strict mode, which makes it less practical for large Python codebases",
        "The error count difference is random because both modes check the exact same set of diagnostic rules"
      ],
      correctOption: 1,
      explanation: "The 6 errors that disappeared in standard mode are not fixed -- they are ignored. Strict mode enables 29 additional diagnostic rules, including reportUnknownParameterType. Standard mode simply does not check for these gaps, creating blind spots where type mismatches hide. Fewer errors does not mean safer code. Option A reverses the relationship. Option C is wrong because analysis speed is comparable. Option D is wrong because strict mode enables significantly more rules. The lesson warns against switching to standard because it shows 'too many errors.'",
      source: "Lesson 5: Pyright -- Your Type Safety Net"
    },
    {
      question: "A developer labels every function parameter as 'Any' to silence pyright errors. Their code now shows zero type errors. A colleague says the code is type-safe. Why is this assessment wrong?",
      options: [
        "Any causes pyright to skip the function entirely, leaving a gap where no type checking happens at all",
        "Any is not valid Python syntax and will cause runtime import errors in production environments",
        "Any forces pyright into basic mode, which disables all other diagnostic rules in the project",
        "Any makes pyright run slower because it must evaluate every possible type combination for each call"
      ],
      correctOption: 0,
      explanation: "Any is a type that matches everything. When every parameter is Any, passing an int where a str belongs is 'valid' by annotation -- pyright sees no mismatch. Zero type safety while maintaining the illusion of typed code. Option B is wrong because Any is valid Python from the typing module. Option C is wrong because using Any does not change pyright's mode. Option D is wrong because Any simplifies analysis -- there is nothing to check. The lesson lists using Any everywhere as a specific anti-pattern.",
      source: "Lesson 5: Pyright -- Your Type Safety Net"
    },
    {
      question: "A developer writes 'def process(data):' without type annotations. In standard mode, pyright shows zero errors. In strict mode, pyright shows 'error: reportUnknownParameterType'. Why does strict mode flag the missing annotation?",
      options: [
        "Strict mode requires annotations so pyright can verify all callers pass the correct data type",
        "Strict mode is designed for legacy codebases that need gradual migration from untyped to typed",
        "Strict mode generates missing annotations automatically and reports them as informational messages",
        "Strict mode converts Python to a statically compiled language that requires type declarations"
      ],
      correctOption: 0,
      explanation: "Without a type annotation on 'data', pyright cannot verify what callers pass to the function. Strict mode flags this as a coverage gap because every unannotated parameter is a place where type mismatches can hide. The annotation is the guardrail -- without it, pyright has nothing to check against. Option B reverses the use case -- standard mode is for gradual migration. Option C is wrong because pyright never modifies source code. Option D is wrong because Python remains interpreted regardless of pyright's mode.",
      source: "Lesson 5: Pyright -- Your Type Safety Net"
    },
    {
      question: "A developer adds type annotations to a function: 'def greet(name: str) -> str'. They ask whether the annotations slow down their program at runtime. What is the correct answer?",
      options: [
        "Yes, Python checks every type annotation at runtime and raises TypeError on any mismatch it finds",
        "Yes, annotations add overhead because Python stores extra metadata for each annotated variable",
        "No, Python completely ignores type annotations at runtime -- they exist only for tools like pyright",
        "No, but only if the code is running inside a virtual environment managed by uv specifically"
      ],
      correctOption: 2,
      explanation: "Python completely ignores type annotations at runtime. They are metadata for tools like pyright and for human readers. Adding ': str' to a parameter does not make Python check types, does not slow execution, and does not change behavior. Option A is wrong because Python never checks annotations at runtime. Option B is wrong because annotation metadata overhead is negligible. Option D is wrong because runtime behavior is independent of virtual environments. The lesson explains annotations serve double duty: documentation for humans and verification for pyright.",
      source: "Lesson 5: Pyright -- Your Type Safety Net"
    },
    {
      question: "Type annotations serve double duty in the discipline stack according to the lesson. What are the two distinct benefits they simultaneously provide?",
      options: [
        "They speed up Python execution and reduce the total amount of memory the program uses at runtime",
        "They prevent unused imports and also enforce consistent code formatting across the entire project",
        "They replace the need for tests by proving function correctness through mathematical type proof logic",
        "They document what each function expects for humans and AI, and enable automated verification by pyright"
      ],
      correctOption: 3,
      explanation: "Type annotations provide two benefits simultaneously: documentation (humans and AI can read function signatures to understand data flow) and verification (pyright checks that every call matches the declared types). One annotation, two purposes. Option A is wrong because Python ignores annotations at runtime -- no performance effect. Option B describes ruff's responsibilities, not type annotations. Option C is wrong because types verify data correctness, not behavioral correctness; tests are still needed. The lesson explicitly describes this double duty.",
      source: "Lesson 5: Pyright -- Your Type Safety Net"
    },
    {
      question: "A developer's pyright is configured in strict mode. They write a function with complete type annotations that passes pyright with zero errors. They also pass all ruff checks. They declare the code is correct. Their colleague writes a test and discovers the function returns wrong values. What does this reveal about the limits of static analysis?",
      options: [
        "It reveals that pyright is unreliable and should be replaced with a more thorough type checking tool",
        "It reveals that ruff and pyright guarantee correct types and structure, but only pytest verifies behavior",
        "It reveals that strict mode should have caught the wrong return value if it was configured more properly",
        "It reveals that static analysis is useless and that only pytest provides any meaningful code verification"
      ],
      correctOption: 1,
      explanation: "This is a fundamental lesson about the pipeline: ruff verifies code structure and style, pyright verifies type correctness, but neither can verify that a function returns the right value for a given input. A function can have perfect types and still compute wrong results. Only pytest catches behavioral bugs by specifying expected outputs for known inputs. Option A is wrong because pyright did its job -- types were correct. Option C is wrong because pyright checks type matching, not return value correctness. Option D is wrong because ruff and pyright provide real value for their categories. All three tools are needed.",
      source: "Lesson 5: Pyright -- Your Type Safety Net"
    },
    {
      question: "A developer writes a pytest test: 'assert main() == \"Hello from smartnotes!\"'. They run 'uv run pytest' and see 'tests/test_main.py . [100%] 1 passed'. They then change main() to return 'Hello from SmartNotes!' (capitalized S). What will pytest show on the next run?",
      options: [
        "The test still passes because pytest ignores capitalization differences in string value comparisons",
        "The test fails with F, showing expected versus actual values highlighting the capitalization mismatch",
        "pytest crashes with an exception because the return value changed from lowercase to mixed case",
        "The test is skipped with s because pytest detects the function was modified since last run"
      ],
      correctOption: 1,
      explanation: "assert uses exact equality comparison. 'Hello from smartnotes!' is not equal to 'Hello from SmartNotes!' because the lowercase 's' differs from uppercase 'S'. pytest will show F (failed), display the expected value with a - prefix and the actual value with a + prefix. Option A is wrong because Python string comparison is case-sensitive. Option C is wrong because pytest does not crash on assertion failures -- it reports them. Option D is wrong because pytest does not track file modification times for skip decisions.",
      source: "Lesson 6: Testing With pytest"
    },
    {
      question: "A developer creates a test file called 'main_test_helpers.py' with a function 'def validate_greet(): assert greet(\"Emma\") == \"Hello, Emma\"'. They run pytest and the test is not discovered. What two naming mistakes did they make?",
      options: [
        "The file should end with .pytest.py and the function should be prefixed with check_ not validate_",
        "The file should be named after the module it tests and the function should include the word assert",
        "The file should be in the project root not the tests directory and the function must have a docstring",
        "The file must match test_*.py or *_test.py, and the function must start with test_ as its prefix"
      ],
      correctOption: 3,
      explanation: "pytest requires specific naming conventions for automatic discovery. Files must match test_*.py or *_test.py -- main_test_helpers.py matches neither pattern because the filename does not end with _test. Functions must start with test_ -- validate_greet does not. Both conventions must be satisfied. Option A fabricates naming conventions. Option B is wrong because pytest uses pattern matching, not module-name matching. Option C is wrong because location and docstrings are not discovery requirements. The lesson lists these conventions in a reference table.",
      source: "Lesson 6: Testing With pytest"
    },
    {
      question: "A developer writes a function 'def add(a: int, b: int) -> int: return a - b' (bug: subtracts instead of adds). They run 'uv run pyright' and see zero errors. They conclude the code is correct. Why is this conclusion wrong?",
      options: [
        "pyright verifies type correctness not behavioral correctness -- valid types but wrong logic in the function",
        "pyright should have caught the subtraction operator as a type error in the function body source code",
        "pyright only checks code in strict mode and the developer was likely running standard mode instead",
        "pyright cannot analyze arithmetic operators and always skips functions that use math operation logic"
      ],
      correctOption: 0,
      explanation: "pyright checks that types match: int - int returns int, which matches the declared return type -> int. The types are correct. But the behavior is wrong -- the function subtracts instead of adding. Only pytest can catch behavioral bugs by asserting 'assert add(3, 2) == 5'. Option B is wrong because a - b with int operands is valid. Option C is wrong because the issue is fundamental to what pyright checks, not the mode. Option D is wrong because pyright handles arithmetic. This illustrates why the pipeline needs all three tools.",
      source: "Lesson 6: Testing With pytest"
    },
    {
      question: "A developer runs pytest and sees 'tests/test_main.py ..F.E [100%]'. They need to explain the output to their team. What do the five characters mean in sequence?",
      options: [
        "Two files checked, one formatted, one file passed, one excluded from the test run entirely",
        "Two tests passed, one test failed, one test passed, one test had an error during execution",
        "Two tests were slow, one test was fast, one test passed, one test was empty and skipped",
        "Two dots indicate progress, F means finished, dot means continuing, E means evaluation complete"
      ],
      correctOption: 1,
      explanation: "Each character represents one test result: '.' means passed, 'F' means failed (assertion was false), and 'E' means error (exception during setup or execution). So ..F.E means: pass, pass, fail, pass, error -- five tests total. Option A fabricates meanings for these characters. Option C invents 'slow' and 'empty' meanings that do not exist in pytest. Option D treats the characters as progress indicators rather than individual test results. The lesson provides a reference table: . = passed, F = failed, E = error, s = skipped.",
      source: "Lesson 6: Testing With pytest"
    },
    {
      question: "The verification pipeline runs 'uv run ruff check . && uv run pyright && uv run pytest'. A developer has a type error in main.py but no lint issues. They run the full pipeline. Which tools execute and which are skipped?",
      options: [
        "All three tools run and the type error appears in the combined output at the very end",
        "Only ruff runs because it detects the type error and stops the pipeline before pyright starts",
        "ruff passes, pyright reports the type error and stops the pipeline, so pytest never runs",
        "ruff and pytest run but pyright is skipped because && only chains first and last commands"
      ],
      correctOption: 2,
      explanation: "The && operator runs each command only if the previous one exited successfully (exit code 0). ruff finds no lint issues and passes. pyright detects the type error and exits non-zero. The && operator stops the pipeline, so pytest never runs. This fail-fast behavior is the point: fix the simplest problem first. Option A is wrong because && stops at first failure. Option B is wrong because ruff does not check types. Option D is wrong because && chains all commands sequentially. The lesson demonstrates this exact scenario.",
      source: "Lesson 7: Git -- Your Version Control Memory"
    },
    {
      question: "James edits his working format_title function, realizes the original was better, and tries to undo. The undo history is gone because he closed the file between versions. He has no Git commits. What should he have done differently to prevent this loss?",
      options: [
        "He should have run 'git add . && git commit -m \"working format_title\"' before starting the rewrite",
        "He should have used pyright to create a checkpoint of the function's type signatures before editing",
        "He should have copied the function to a separate backup file before making any changes to it",
        "He should have run the function through pytest first so the test output would preserve the code"
      ],
      correctOption: 0,
      explanation: "A git commit before rewriting would have recorded the working function as a permanent snapshot. If the rewrite went wrong, James could recover with git log and git checkout. This is Axiom VIII (Version Control is Memory) in practice. Option C works in theory but is fragile and does not scale. Option B is wrong because pyright checks types, not snapshots. Option D is wrong because pytest output shows pass/fail results, not source code. The lesson opens with this exact scenario as the motivation for Git.",
      source: "Lesson 7: Git -- Your Version Control Memory"
    },
    {
      question: "A developer runs only pytest before committing. All tests pass with green dots. Their code has three unused imports and two type errors. They push to the shared repository. What was their mistake?",
      options: [
        "They should have run pytest with the --strict flag to enable import and type checking during tests",
        "They should have run the full pipeline: ruff check, then pyright, then pytest before committing",
        "They should have asked a teammate to review the code visually before pushing to the repository",
        "They should have run pytest twice to confirm the results were not caused by flaky test behavior"
      ],
      correctOption: 1,
      explanation: "Each pipeline tool catches a different category: ruff catches unused imports and style violations, pyright catches type mismatches, and pytest catches behavioral bugs. Running only pytest means lint violations and type errors go undetected. The full pipeline ensures all categories are verified. Option A is wrong because pytest has no --strict flag for import/type checking. Option C adds manual review but misses what automated tools find. Option D catches flaky tests, not missing ruff and pyright checks.",
      source: "Lesson 7: Git -- Your Version Control Memory"
    },
    {
      question: "A developer has been working for three hours without committing. Their laptop battery dies during a forced restart, and unsaved changes are lost. According to the discipline stack, what should they have done differently?",
      options: [
        "They should have installed a battery monitoring tool that automatically commits code at low battery levels",
        "They should have used a cloud-based IDE that saves every keystroke to prevent any possible data loss",
        "They should have committed after every meaningful change, creating small and frequent Git checkpoint saves",
        "They should have emailed the code to themselves as a backup after every hour of completed work"
      ],
      correctOption: 2,
      explanation: "The 'not committing early' anti-pattern means working for extended periods without Git checkpoints. Committing after every meaningful change creates recovery points. If the battery dies after the latest commit, at most one small change is lost. Option A is fabricated. Option B introduces external dependencies instead of using Git. Option D is a primitive backup method without version history. The lesson lists 'not committing early' as a specific anti-pattern.",
      source: "Lesson 7: Git -- Your Version Control Memory"
    },
    {
      question: "A developer runs 'git init' in SmartNotes, then 'git add .' and 'git commit -m \"initial project\"'. The next day they accidentally delete main.py and panic. What Git command can recover the deleted file from the commit?",
      options: [
        "git pull origin main to download the file from the remote server that automatically saved everything",
        "git checkout HEAD -- main.py to restore the file from the most recent commit snapshot in history",
        "git stash pop to retrieve the file from Git's automatic backup clipboard memory buffer storage",
        "git reset --hard to revert the entire repository to its factory default empty initial state"
      ],
      correctOption: 1,
      explanation: "git checkout HEAD -- main.py restores main.py from the most recent commit without affecting other files. This is Axiom VIII (Version Control is Memory) in practice -- the commit recorded the file permanently. Option A is wrong because no remote server was set up. Option C is wrong because git stash is for temporarily shelving changes, not recovering from commits. Option D is destructive and would discard all uncommitted changes without selectively restoring one file. The lesson explains that commits are permanent checkpoints.",
      source: "Lesson 7: Git -- Your Version Control Memory"
    },
    {
      question: "A developer verifies their code by running it manually and checking the terminal output by eye. Their teammate writes pytest assertions checking the same scenarios. Both approaches confirm the code works today. Which approach provides value when the code changes next week?",
      options: [
        "Manual testing is more valuable because the developer can spot subtle visual differences in output",
        "Both approaches provide equal value because they verified the same scenarios with equal thoroughness",
        "Neither approach provides value next week because both need to be re-executed from scratch again",
        "The pytest assertions provide lasting value because they re-run automatically and catch regressions"
      ],
      correctOption: 3,
      explanation: "Manual testing verifies one scenario, one time, and leaves no record. When code changes, the developer must manually re-check everything -- if they remember it all. pytest assertions are permanent specifications that re-run automatically. If an edit breaks a scenario, the assertion catches it instantly. Option A is wrong because manual testing does not scale. Option B is wrong because manual testing creates no reusable artifact. Option C is wrong because pytest assertions re-execute without being re-created. The lesson contrasts manual testing with automated assertions.",
      source: "Lesson 6: Testing With pytest"
    }
  ]}
  questionsPerBatch={20}
/>
